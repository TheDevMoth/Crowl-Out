/// Define macros and enums to be used throughout the game

//Controls
#macro LEFT (keyboard_check(vk_left) || keyboard_check(ord("A")))
#macro RIGHT (keyboard_check(vk_right) || keyboard_check(ord("D")))
#macro UP (keyboard_check(vk_up) || keyboard_check(ord("W")))
#macro DOWN (keyboard_check(vk_down) || keyboard_check(ord("S")))
#macro RUN (keyboard_check(vk_shift))

//Tiles
#macro TILE_W 16
#macro TILE_H 16

//Camera

